// 这里存放的是代码模板

<% if (express) { %>
// 创建express
const express = require('express')
const a = express.a()
<% } %>  

<% if (router) { %>
// 创建router
// 创建express
const router = require('router')
const a = router.a()
<% } %>  
